import axios from "axios";
import { callApi } from "../../utils/utils";

import {
  LOAD_GIRLS_DATA,
  LOADING,
  LOADING_SUCCESS,
  LOADING_FAIL
} from "./index";

export const loadGirlsData = () => {
  return async (dispatch, getState) => {
  };
};